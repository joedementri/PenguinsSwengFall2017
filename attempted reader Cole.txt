List<String^> reader(filename)
{
	List<String^> declared = gcnew List<String^>(); //list of declared variables
	std::string temp = "";
	filename="C:\\Users\dougl\Desktop\PenguinsSwengFall2017-master (10)\PenguinsSwengFall2017-master\Ada Files\fibonacci_BAD.adb"
		System::IO::StreamReader^ sr = gcnew System::IO::StreamReader(filename);
		temp = sr->ReadToEnd();
		sr->Close();
	


	std::istringstream buf(temp);
	std::istream_iterator<std::string> beg(buf), end;
	std::vector<std::string> tokens(beg, end); // done!
		for (auto& s : tokens)
			declared.add(s);

	return declared;
}